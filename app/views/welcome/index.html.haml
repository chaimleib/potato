%p POTATO is a developer dashboard that gathers together JIRA and GitHub data in a single, convenient place. 
%p The acronym stands for Programmer's Open Task AgregaTOr.
%h2 Views
%ul
  - PotatoController.instance_methods(false).each do |sym|
  - next if sym == :index
  - name = sym.to_s
  - display_name = controller.action_display_name name
    %li
      = link_to display_name, "/potato/#{name}"
%h2 Admin
%ul
  - @admin_controllers.each do |ctlr|
    %li= link_to ctlr.controller_display_name, ctlr.controller_path  
